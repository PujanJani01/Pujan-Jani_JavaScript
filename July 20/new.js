/* const arr = [
    { name: 'John', location: 'Los Angeles' },
    { name: 'Kate', location: 'New York' },
    { name: 'Kate', location: 'New York' },
    { name: 'Kate', location: 'LA' },
    { name: 'Mike', location: 'New York' },
  ];
  
  const unique = [];
  arr.forEach((obj) => {
    const found = unique.findIndex((item) => item.name === obj.name && item.location === obj.location);
    if (found === -1) {
      unique.push(obj);
    }
  });
  console.log(unique);  */
  